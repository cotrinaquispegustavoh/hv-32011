# Incluir dentro del bloque `server` HTTPS de intranet-hv.sytes.net.
# /media/ debe continuar llegando a Django mediante el `location /` existente.
location /_protected_media/ {
    internal;
    alias /var/www/intranet-hv/media/;
    add_header X-Content-Type-Options "nosniff" always;
}
